server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: bills-to-pay
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    url: jdbc:mysql://localhost:3306/bills_to_pay
    username: root
    password: 1089